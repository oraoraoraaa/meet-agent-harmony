/**
 * MeetAgent visual tokens — dark map / glass nav style (zh-CN primary).
 * Approximate palette from product mock (Gaode-like results + glass inputs).
 */

export class AppTheme {
  // Canvas
  static readonly bg: string = '#0B1220';
  static readonly bgElevated: string = '#111827';
  static readonly card: string = '#1E293B';
  static readonly cardSelected: string = '#0F766E';
  static readonly cardMuted: string = '#334155';
  static readonly glass: string = '#E6F7F5';
  static readonly glassSoft: string = '#F0FDFA';
  static readonly mapPanel: string = '#E8EEF5';
  static readonly mapGrid: string = '#CBD5E1';

  // Accents
  static readonly primary: string = '#22C55E';
  static readonly primaryDeep: string = '#16A34A';
  static readonly teal: string = '#14B8A6';
  static readonly tealDeep: string = '#0D9488';
  static readonly blue: string = '#60A5FA';
  static readonly purple: string = '#A78BFA';
  static readonly amber: string = '#F59E0B';
  static readonly danger: string = '#F87171';
  static readonly pinkSoft: string = '#FCE7F3';

  // Text
  static readonly text: string = '#F8FAFC';
  static readonly textSecondary: string = '#94A3B8';
  static readonly textMuted: string = '#64748B';
  static readonly textOnLight: string = '#0F172A';
  static readonly textOnLightSecondary: string = '#475569';
  static readonly textOnPrimary: string = '#FFFFFF';

  // Borders / chrome
  static readonly border: string = '#334155';
  static readonly borderSoft: string = '#1F2937';
  static readonly hairline: string = '#E2E8F0';
}
