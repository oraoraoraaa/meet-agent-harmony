# 月度激励 - 星光应用申报

**【应用名称】：** 会合助手（MeetAgent）

**【应用类型】：** HarmonyOS 原生 AI 出行效率工具

**【一句话简介】：** 面向司机接驾场景的会合策略助手，在行程开始时基于地图数据与本地算法生成可解释的最优会合方案，并支持一键锁定、分享和导航。

**【应用背景与痛点】：**
- 传统固定上车点常因实时路况变化导致司机绕行、乘客等待时间增加。
- 用户通常缺少“司机到达时间 + 乘客步行/骑行/公交成本”的联合视角。
- 仅给结果不给依据的黑盒推荐难以建立信任。

**【核心功能】：**
1. **多模态会合方案生成**：支持“原地等待 / 步行 / 骑行 / 公交”多方案对比。
2. **本地确定性引擎决策**：基于候选点生成、可达性判断、统一评分模型输出推荐。
3. **AI 助手约束理解**：支持自然语言约束（如“最多走8分钟，不坐公交”）并转为结构化条件。
4. **可解释推荐**：推荐理由与分钟数、候选点、路线等工具结果一致，不虚构时间与坐标。
5. **方案锁定机制（v1）**：用户确认后锁定会合点，不自动漂移；仅支持手动重规划。
6. **分享与导航执行**：支持复制分享文案与打开地图 App 执行落地。
7. **离线降级可用**：无 LLM Key 或网络异常时，仍可走本地估算模式完成规划。

**【技术架构与实现亮点】：**
- **客户端主导架构**：HarmonyOS（ArkTS/ArkUI）为主产品面，仓库根目录为 DevEco 工程。
- **AI + 工具协同**：LLM 仅负责理解与解释，路线/ETA/候选评分由工具与引擎提供。
- **三模式 LLM 接入**：用户直连（Mode A）/代理（Mode B）/离线（Mode C）兼容。
- **地图能力混合策略**：`HybridMapProvider` 优先实时能力，失败自动回落估算并标注数据来源。
- **纯领域引擎可测试**：`domain/` 保持 UI 无关、可单测，保障可维护性与可靠性。

**【技术栈】：**
- **OS 与客户端**：HarmonyOS、ArkTS、ArkUI（Stage 模型）
- **领域层**：TypeScript 纯函数引擎（候选点、评分、排序、阈值决策）
- **地图与路线**：AMap Web REST + 估算路由回退
- **AI 接口**：OpenAI 兼容 Chat Completions（兼容 DeepSeek/Qwen/OpenAI/自建网关）
- **可选服务端**：轻量代理（`server/`）用于 Demo Key 托管

**【创新点与差异化价值】：**
- 从“固定上车点”升级为“会合策略优化”，面向真实接驾效率问题。
- 通过“司机成本 + 乘客负担”联合评分，避免单边最优。
- 强约束防幻觉：AI 不生成定量结果，只使用工具返回的真实数据。
- 在弱网/无 Key 场景仍可用，具备稳定演示与实际落地能力。

**【当前完成度】：**
- 已形成完整主流程：选点/输入约束 → 生成多方案 → 推荐解释 → 锁定 → 分享/导航。
- 具备离线与在线双路径，支持 Demo Fixture 稳定演示。
- 领域与 Agent 关键逻辑已有配套测试，文档体系（产品/架构/AI/实施计划）完整。

**【应用价值】：**
- **用户价值**：减少会合等待、提升接驾效率、降低沟通成本。
- **平台价值**：体现 HarmonyOS 原生 AI 应用在出行决策场景的工程化落地能力。
- **示范价值**：可复制到通勤接驳、活动接人、园区摆渡等“会合优化”场景。

**【后续规划】：**
- 完善页面动效与品牌化信息（图标/About）。
- 继续提升多语言体验与赛场演示稳定性。
- 在保持 v1“手动重规划”边界下，为后续扩展预留能力接口。
